import { css } from '@emotion/css'
import React, { FC } from 'react'

export const Main: FC = () => {
  return (
    <div
      className={css`
        padding: 10px 20px;
      `}
    >
      <div></div>
    </div>
  )
}
